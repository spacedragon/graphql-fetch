
query test($first: Int!) {
    allShows(first: $first) {
         ...showFields
    }
}

mutation createUser($name: String!, $alias: String!) {
    createUser(name: $name, alias: $alias){
        id
        name
        alias
    }
}